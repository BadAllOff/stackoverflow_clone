var flashMessages = $('.flash-messages');
flashMessages.empty();

<% if flash[:success].present? %>
  flashMessages.html('<%= flash_messages(flash[:success]) %>');
<% end %>
$('li#attachment-<%= @attachment.id %>').remove();
<% flash.discard %>